title = "Gitleaks config - OpenTrade / ZeusPrime"

[allowlist]
  description = "Allow Polygon EVM contract addresses and known false positives"
  regexes = [
    "0x[0-9a-fA-F]{40}",
    "https://api.dexpaprika.com",
  ]
  paths = [
    "arb_prime.py",
    "zeus_prime_v2.py",
  ]
